meta {
  name: Update Genre
  type: http
  seq: 4
}

put {
  url: {{protocol}}/{{host}}:{{port}}/api/game/genres/{{genreId}}/
  body: multipartForm
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

body:multipart-form {
  name: Test Gen
}
