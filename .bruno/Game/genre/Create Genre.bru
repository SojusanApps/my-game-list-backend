meta {
  name: Create Genre
  type: http
  seq: 2
}

post {
  url: {{protocol}}/{{host}}:{{port}}/api/game/genres/
  body: multipartForm
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

body:multipart-form {
  name: Test Genre
}
