meta {
  name: Delete Genre
  type: http
  seq: 6
}

delete {
  url: {{protocol}}/{{host}}:{{port}}/api/game/genres/{{genreId}}/
  body: none
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
